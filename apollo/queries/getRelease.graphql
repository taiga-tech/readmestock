query {
  viewer {
    repository(name: "readmestock") {
      createdAt
      releases(first: 5) {
        nodes {
          publishedAt
          name
          description
          descriptionHTML
          url
          isLatest
        }
        totalCount
      }
    }
  }
}
