query {
  user(login: "taiga-tech") {
    url
    login
    avatarUrl
    repositories(first: 100) {
      nodes {
        languages(first: 100) {
          nodes {
            name
            color
          }
        }
      }
      totalCount
    }
  }
}
